---
/**
 * Moatrics homepage — Phase A.
 *
 * Sections (top → bottom):
 *   1. Hero with animated purple/gold gradient blobs
 *   2. Trust band ("Built around the principles…")
 *   3. Features grid (6 cards with inline SVG icons)
 *   4. How It Works (3-step process)
 *   5. Metrics band (ROIC / Debt / Growth / Margin of Safety)
 *   6. Pricing teaser (Free + Pro, monthly/annual toggle — vanilla JS)
 *   7. CTA banner
 *
 * All copy is sourced from `src/i18n/en.json` via the dotted-path `t()` helper.
 * Auth links go to https://app.moatrics.com.
 */
import PublicLayout from '../layouts/PublicLayout.astro';
import { t, getNode } from '../i18n';

const APP_BASE = 'https://app.moatrics.com';

const featureKeys = ['moat', 'valuation', 'screener', 'watchlist', 'gurus', 'journal'] as const;
const stepKeys = ['screen', 'analyze', 'decide'] as const;
const metricKeys = ['roic', 'debt', 'growth', 'valuation'] as const;

// Pull Pro feature list from i18n (it's an array of strings)
const freeFeatures = (getNode('home.pricing_teaser.free.features') as string[]) ?? [];
const proFeatures = (getNode('home.pricing_teaser.pro.features') as string[]) ?? [];

const title = 'Moatrics — Invest in Quality Companies. Undervalued.';
const description =
  'Moatrics is a data-driven co-pilot for long-term, value-conscious investors. Quality businesses, durable moats, margin of safety.';
---

<PublicLayout title={title} description={description} path="/">
  {/* ─── 1. HERO ────────────────────────────────────────────────────────── */}
  <section class="mx-hero">
    <div class="mx-hero-blob mx-hero-blob-a" aria-hidden="true"></div>
    <div class="mx-hero-blob mx-hero-blob-b" aria-hidden="true"></div>

    <div class="mx-container mx-hero-inner">
      <span class="mx-eyebrow">{t('home.hero.eyebrow')}</span>
      <h1 class="mx-hero-title">{t('home.hero.title')}</h1>
      <p class="mx-hero-subtitle">{t('home.hero.subtitle')}</p>
      <div class="mx-hero-cta">
        <a class="mx-btn mx-btn-primary" href={`${APP_BASE}/signup`}>
          {t('home.hero.cta_primary')}
        </a>
        <a class="mx-btn mx-btn-secondary" href="#how-it-works">
          {t('home.hero.cta_secondary')}
        </a>
      </div>
    </div>
  </section>

  {/* ─── 2. TRUST BAND ──────────────────────────────────────────────────── */}
  <section class="mx-trust">
    <div class="mx-container mx-trust-inner">
      <h2 class="mx-trust-title">{t('home.trust.title')}</h2>
      <p class="mx-trust-sub">{t('home.trust.subtitle')}</p>
    </div>
  </section>

  {/* ─── 3. FEATURES ─────────────────────────────────────────────────────── */}
  <section class="mx-section" id="features">
    <div class="mx-container">
      <div class="mx-section-head">
        <span class="mx-eyebrow mx-eyebrow-purple">{t('home.features.section_eyebrow')}</span>
        <h2 class="mx-section-title">{t('home.features.section_title')}</h2>
        <p class="mx-section-sub">{t('home.features.section_subtitle')}</p>
      </div>

      <div class="mx-features-grid">
        {featureKeys.map((k) => (
          <article class="mx-feature mx-glass">
            <div class="mx-feature-icon" aria-hidden="true">
              {k === 'moat' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M3 21V10l9-7 9 7v11" />
                  <path d="M9 21v-6h6v6" />
                  <path d="M3 14h18" />
                </svg>
              )}
              {k === 'valuation' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M12 3v18" />
                  <path d="M5 8h14" />
                  <path d="M5 8l-2 6a4 4 0 0 0 8 0L9 8" />
                  <path d="M19 8l-2 6a4 4 0 0 0 8 0l-2-6" />
                </svg>
              )}
              {k === 'screener' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M3 5h18l-7 8v6l-4-2v-4z" />
                </svg>
              )}
              {k === 'watchlist' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="12" cy="12" r="3" />
                  <path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12z" />
                </svg>
              )}
              {k === 'gurus' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="9" cy="8" r="4" />
                  <path d="M2 21v-2a5 5 0 0 1 5-5h4a5 5 0 0 1 5 5v2" />
                  <circle cx="17" cy="6" r="3" />
                  <path d="M22 21v-1a4 4 0 0 0-3-3.9" />
                </svg>
              )}
              {k === 'journal' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M4 4h13a3 3 0 0 1 3 3v13a0 0 0 0 1 0 0H7a3 3 0 0 1-3-3z" />
                  <path d="M4 4v13a3 3 0 0 0 3 3" />
                  <path d="M8 8h8M8 12h8M8 16h5" />
                </svg>
              )}
            </div>
            <h3 class="mx-feature-title">{t(`home.features.items.${k}.title`)}</h3>
            <p class="mx-feature-desc">{t(`home.features.items.${k}.desc`)}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  {/* ─── 4. HOW IT WORKS ─────────────────────────────────────────────────── */}
  <section class="mx-section" id="how-it-works">
    <div class="mx-container">
      <div class="mx-section-head">
        <span class="mx-eyebrow">{t('home.how_it_works.section_eyebrow')}</span>
        <h2 class="mx-section-title">{t('home.how_it_works.section_title')}</h2>
        <p class="mx-section-sub">{t('home.how_it_works.section_subtitle')}</p>
      </div>

      <div class="mx-steps-grid">
        {stepKeys.map((k, i) => (
          <article class="mx-step mx-glass">
            <span class="mx-step-label">{t(`home.how_it_works.steps.${k}.label`)}</span>
            <h3 class="mx-step-title">{t(`home.how_it_works.steps.${k}.title`)}</h3>
            <p class="mx-step-desc">{t(`home.how_it_works.steps.${k}.desc`)}</p>
            {i < stepKeys.length - 1 && (
              <div class="mx-step-arrow" aria-hidden="true">→</div>
            )}
          </article>
        ))}
      </div>
    </div>
  </section>

  {/* ─── 5. METRICS BAND ─────────────────────────────────────────────────── */}
  <section class="mx-section-tight">
    <div class="mx-container">
      <h2 class="mx-section-title mx-metrics-title">{t('home.metrics.section_title')}</h2>
      <div class="mx-metrics-grid">
        {metricKeys.map((k) => (
          <div class="mx-metric">
            <span class="mx-metric-label">{t(`home.metrics.items.${k}.label`)}</span>
            <p class="mx-metric-desc">{t(`home.metrics.items.${k}.desc`)}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  {/* ─── 6. PRICING TEASER ───────────────────────────────────────────────── */}
  <section class="mx-section" id="pricing">
    <div class="mx-container">
      <div class="mx-section-head">
        <span class="mx-eyebrow">{t('home.pricing_teaser.section_eyebrow')}</span>
        <h2 class="mx-section-title">{t('home.pricing_teaser.section_title')}</h2>
        <p class="mx-section-sub">{t('home.pricing_teaser.section_subtitle')}</p>
      </div>

      <div class="mx-billing-toggle" role="tablist" aria-label="Billing cycle">
        <button
          type="button"
          role="tab"
          aria-selected="true"
          class="mx-billing-btn mx-billing-active"
          data-billing="monthly"
        >
          {t('home.pricing_teaser.billing_monthly')}
        </button>
        <button
          type="button"
          role="tab"
          aria-selected="false"
          class="mx-billing-btn"
          data-billing="annually"
        >
          {t('home.pricing_teaser.billing_annually')}
        </button>
      </div>

      <div class="mx-pricing-grid">
        {/* Free */}
        <article class="mx-price-card mx-glass">
          <header class="mx-price-head">
            <h3 class="mx-price-name">{t('home.pricing_teaser.free.name')}</h3>
            <div class="mx-price-amount">
              <span class="mx-price-num">{t('home.pricing_teaser.free.price')}</span>
              <span class="mx-price-period">{t('home.pricing_teaser.free.period')}</span>
            </div>
            <p class="mx-price-tag">{t('home.pricing_teaser.free.tagline')}</p>
          </header>
          <ul role="list" class="mx-price-list">
            {freeFeatures.map((f) => (
              <li>
                <span class="mx-price-check" aria-hidden="true">✓</span>
                <span>{f}</span>
              </li>
            ))}
          </ul>
          <a class="mx-btn mx-btn-secondary mx-price-cta" href={`${APP_BASE}/signup`}>
            {t('home.pricing_teaser.free.cta')}
          </a>
        </article>

        {/* Pro */}
        <article class="mx-price-card mx-price-card-featured mx-glass">
          <span class="mx-price-badge">Recommended</span>
          <header class="mx-price-head">
            <h3 class="mx-price-name">{t('home.pricing_teaser.pro.name')}</h3>
            <div class="mx-price-amount">
              <span
                class="mx-price-num"
                data-monthly={t('home.pricing_teaser.pro.price_monthly')}
                data-annually={t('home.pricing_teaser.pro.price_annually')}
              >
                {t('home.pricing_teaser.pro.price_monthly')}
              </span>
              <span class="mx-price-period">{t('home.pricing_teaser.pro.period')}</span>
            </div>
            <p
              class="mx-price-tag"
              data-monthly={t('home.pricing_teaser.pro.billing_note_monthly')}
              data-annually={t('home.pricing_teaser.pro.billing_note_annually')}
            >
              {t('home.pricing_teaser.pro.billing_note_monthly')}
            </p>
            <p class="mx-price-tag">{t('home.pricing_teaser.pro.tagline')}</p>
          </header>
          <ul role="list" class="mx-price-list">
            {proFeatures.map((f) => (
              <li>
                <span class="mx-price-check" aria-hidden="true">✓</span>
                <span>{f}</span>
              </li>
            ))}
          </ul>
          <a class="mx-btn mx-btn-primary mx-price-cta" href={`${APP_BASE}/signup`}>
            {t('home.pricing_teaser.pro.cta')}
          </a>
        </article>
      </div>
    </div>
  </section>

  {/* ─── 7. CTA BANNER ───────────────────────────────────────────────────── */}
  <section class="mx-section-tight">
    <div class="mx-container">
      <div class="mx-cta-banner mx-glass">
        <h2 class="mx-cta-title">{t('home.cta_banner.title')}</h2>
        <p class="mx-cta-sub">{t('home.cta_banner.subtitle')}</p>
        <a class="mx-btn mx-btn-primary" href={`${APP_BASE}/signup`}>
          {t('home.cta_banner.button')}
        </a>
      </div>
    </div>
  </section>
</PublicLayout>

<style>
  /* ─── Hero ───────────────────────────────────────────────────────────── */
  .mx-hero {
    position: relative;
    padding: 120px 0 96px 0;
    overflow: hidden;
  }
  .mx-hero-inner {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 880px;
  }
  .mx-hero-title {
    font-family: var(--mx-font-display);
    font-weight: 700;
    font-size: clamp(2.4rem, 6vw, 4.4rem);
    line-height: 1.04;
    letter-spacing: -2px;
    margin: 0 0 22px 0;
    color: var(--mx-text);
    background: linear-gradient(135deg, #fafafa 0%, #d4d4d8 60%, #fbbf24 110%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .mx-hero-subtitle {
    font-family: var(--mx-font-body);
    font-size: clamp(1.05rem, 1.5vw, 1.2rem);
    color: var(--mx-text-muted);
    max-width: 620px;
    margin: 0 auto 36px auto;
    line-height: 1.55;
  }
  .mx-hero-cta {
    display: inline-flex;
    gap: 14px;
    flex-wrap: wrap;
    justify-content: center;
  }

  .mx-hero-blob {
    position: absolute;
    z-index: 0;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.6;
    pointer-events: none;
  }
  .mx-hero-blob-a {
    width: 520px;
    height: 520px;
    background: radial-gradient(circle, rgba(168, 85, 247, 0.45) 0%, transparent 70%);
    top: -120px;
    left: -120px;
    animation: mxFloatA 18s ease-in-out infinite;
  }
  .mx-hero-blob-b {
    width: 460px;
    height: 460px;
    background: radial-gradient(circle, rgba(251, 191, 36, 0.32) 0%, transparent 70%);
    bottom: -140px;
    right: -100px;
    animation: mxFloatB 22s ease-in-out infinite;
  }
  @keyframes mxFloatA {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(40px, 30px) scale(1.08); }
  }
  @keyframes mxFloatB {
    0%, 100% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(-30px, -40px) scale(1.1); }
  }

  /* ─── Trust band ─────────────────────────────────────────────────────── */
  .mx-trust {
    padding: 32px 0 16px 0;
  }
  .mx-trust-inner {
    text-align: center;
    max-width: 780px;
  }
  .mx-trust-title {
    font-family: var(--mx-font-display);
    font-size: clamp(1.2rem, 2vw, 1.55rem);
    font-weight: 600;
    color: var(--mx-text-2);
    margin: 0 0 8px 0;
    line-height: 1.4;
    letter-spacing: -0.4px;
  }
  .mx-trust-sub {
    font-family: var(--mx-font-mono);
    font-size: 0.82rem;
    letter-spacing: 2.4px;
    text-transform: uppercase;
    color: var(--mx-gold);
    margin: 0;
  }

  /* ─── Features grid ──────────────────────────────────────────────────── */
  .mx-features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }
  .mx-feature {
    padding: 32px 28px;
    transition: transform 0.22s ease, border-color 0.22s ease;
  }
  .mx-feature:hover {
    transform: translateY(-3px);
    border-color: rgba(168, 85, 247, 0.3);
  }
  .mx-feature-icon {
    width: 48px;
    height: 48px;
    border-radius: 14px;
    background: rgba(168, 85, 247, 0.08);
    border: 1px solid var(--mx-border-strong);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: var(--mx-gold);
    margin-bottom: 20px;
  }
  .mx-feature-icon svg { width: 24px; height: 24px; }
  .mx-feature-title {
    font-family: var(--mx-font-display);
    font-size: 1.18rem;
    font-weight: 600;
    color: var(--mx-text);
    margin: 0 0 10px 0;
    letter-spacing: -0.3px;
  }
  .mx-feature-desc {
    font-family: var(--mx-font-body);
    font-size: 0.95rem;
    color: var(--mx-text-muted);
    line-height: 1.6;
    margin: 0;
  }
  @media (max-width: 900px) {
    .mx-features-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 560px) {
    .mx-features-grid { grid-template-columns: 1fr; }
  }

  /* ─── How it works ───────────────────────────────────────────────────── */
  .mx-steps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }
  .mx-step {
    position: relative;
    padding: 32px 28px;
  }
  .mx-step-label {
    font-family: var(--mx-font-mono);
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 2.2px;
    text-transform: uppercase;
    color: var(--mx-gold);
    display: block;
    margin-bottom: 14px;
  }
  .mx-step-title {
    font-family: var(--mx-font-display);
    font-size: 1.35rem;
    font-weight: 600;
    color: var(--mx-text);
    margin: 0 0 12px 0;
    letter-spacing: -0.4px;
  }
  .mx-step-desc {
    font-family: var(--mx-font-body);
    font-size: 0.95rem;
    color: var(--mx-text-muted);
    line-height: 1.6;
    margin: 0;
  }
  .mx-step-arrow {
    position: absolute;
    top: 50%;
    right: -16px;
    transform: translateY(-50%);
    font-size: 1.6rem;
    color: var(--mx-purple-2);
    opacity: 0.6;
    z-index: 2;
  }
  @media (max-width: 900px) {
    .mx-steps-grid { grid-template-columns: 1fr; }
    .mx-step-arrow { display: none; }
  }

  /* ─── Metrics ────────────────────────────────────────────────────────── */
  .mx-metrics-title {
    margin-bottom: 48px;
  }
  .mx-metrics-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 18px;
  }
  .mx-metric {
    padding: 24px 22px;
    border-radius: var(--mx-radius);
    border: 1px solid var(--mx-border-strong);
    background: rgba(168, 85, 247, 0.04);
  }
  .mx-metric-label {
    display: block;
    font-family: var(--mx-font-display);
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--mx-gold);
    margin-bottom: 8px;
    letter-spacing: -0.2px;
  }
  .mx-metric-desc {
    font-family: var(--mx-font-body);
    font-size: 0.88rem;
    color: var(--mx-text-muted);
    margin: 0;
    line-height: 1.5;
  }
  @media (max-width: 900px) {
    .mx-metrics-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 480px) {
    .mx-metrics-grid { grid-template-columns: 1fr; }
  }

  /* ─── Pricing ────────────────────────────────────────────────────────── */
  .mx-billing-toggle {
    display: inline-flex;
    margin: 0 auto 40px auto;
    padding: 4px;
    background: rgba(168, 85, 247, 0.06);
    border: 1px solid var(--mx-border-strong);
    border-radius: 999px;
    justify-content: center;
  }
  .mx-section .mx-billing-toggle {
    display: flex;
    width: fit-content;
    margin-left: auto;
    margin-right: auto;
  }
  .mx-billing-btn {
    min-height: 40px;
    padding: 8px 22px;
    background: transparent;
    border: none;
    border-radius: 999px;
    font-family: var(--mx-font-body);
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--mx-text-muted);
    cursor: pointer;
    transition: background 0.18s ease, color 0.18s ease;
  }
  .mx-billing-btn.mx-billing-active {
    background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
    color: #1a0f00;
    font-weight: 600;
  }

  .mx-pricing-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
    max-width: 880px;
    margin: 0 auto;
  }
  .mx-price-card {
    position: relative;
    padding: 36px 32px;
    display: flex;
    flex-direction: column;
  }
  .mx-price-card-featured {
    border-color: var(--mx-border-gold);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.04) inset,
      0 8px 32px rgba(0, 0, 0, 0.45),
      0 0 0 1px rgba(251, 191, 36, 0.18);
  }
  .mx-price-badge {
    position: absolute;
    top: -12px;
    right: 24px;
    padding: 4px 14px;
    background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
    color: #1a0f00;
    font-family: var(--mx-font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    border-radius: 999px;
  }
  .mx-price-head { margin-bottom: 22px; }
  .mx-price-name {
    font-family: var(--mx-font-display);
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--mx-text);
    margin: 0 0 14px 0;
    letter-spacing: -0.3px;
  }
  .mx-price-amount {
    display: flex;
    align-items: baseline;
    gap: 8px;
    margin-bottom: 8px;
  }
  .mx-price-num {
    font-family: var(--mx-font-display);
    font-size: 2.6rem;
    font-weight: 700;
    color: var(--mx-text);
    letter-spacing: -1.4px;
    line-height: 1;
  }
  .mx-price-period {
    font-family: var(--mx-font-body);
    font-size: 0.95rem;
    color: var(--mx-text-muted);
  }
  .mx-price-tag {
    font-family: var(--mx-font-body);
    font-size: 0.88rem;
    color: var(--mx-text-muted);
    margin: 0 0 6px 0;
  }
  .mx-price-list {
    list-style: none;
    padding: 0;
    margin: 0 0 24px 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
    flex: 1 1 auto;
  }
  .mx-price-list li {
    display: flex;
    gap: 10px;
    align-items: flex-start;
    font-family: var(--mx-font-body);
    font-size: 0.93rem;
    color: var(--mx-text-2);
    line-height: 1.5;
  }
  .mx-price-check {
    color: var(--mx-gold);
    font-weight: 700;
    flex: 0 0 auto;
  }
  .mx-price-cta { width: 100%; }

  @media (max-width: 720px) {
    .mx-pricing-grid { grid-template-columns: 1fr; }
  }

  /* ─── CTA banner ─────────────────────────────────────────────────────── */
  .mx-cta-banner {
    text-align: center;
    padding: 64px 32px;
    border-color: var(--mx-border-gold);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.04) inset,
      0 8px 32px rgba(0, 0, 0, 0.45),
      0 0 60px rgba(251, 191, 36, 0.08);
  }
  .mx-cta-title {
    font-family: var(--mx-font-display);
    font-size: clamp(1.6rem, 3.2vw, 2.4rem);
    font-weight: 700;
    color: var(--mx-text);
    margin: 0 0 14px 0;
    letter-spacing: -1px;
    line-height: 1.15;
  }
  .mx-cta-sub {
    font-family: var(--mx-font-body);
    font-size: 1rem;
    color: var(--mx-text-muted);
    max-width: 560px;
    margin: 0 auto 28px auto;
    line-height: 1.6;
  }
</style>

<script is:inline>
  // Vanilla JS billing toggle (Free is static, Pro price swaps between
  // data-monthly/data-annually values). No framework, no client hydration.
  (function () {
    const buttons = document.querySelectorAll('.mx-billing-btn');
    const swappable = document.querySelectorAll('[data-monthly][data-annually]');
    if (!buttons.length || !swappable.length) return;

    const setBilling = (mode) => {
      buttons.forEach((b) => {
        const active = b.getAttribute('data-billing') === mode;
        b.classList.toggle('mx-billing-active', active);
        b.setAttribute('aria-selected', String(active));
      });
      swappable.forEach((el) => {
        const next = el.getAttribute(`data-${mode}`);
        if (next != null) el.textContent = next;
      });
    };

    buttons.forEach((b) => {
      b.addEventListener('click', () => {
        const mode = b.getAttribute('data-billing');
        if (mode) setBilling(mode);
      });
    });
  })();
</script>
